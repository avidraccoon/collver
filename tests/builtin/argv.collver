include "std.collver"

proc main int ptr -> int do
  // Need to fix the order of arguments in side the compiler but this is a workaround
  [int,ptr->ptr,int]
  print @ptr puts "\n" (str->ptr) puts
  0
end
